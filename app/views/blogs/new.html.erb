<h2>Add a New Blog Post</h2>

<div class="blog-entry">
<%= form_for @blog do |f| %>

<% if @blog.errors.any? %>
  <div id="error_explanation">
    <h2>
      <%= pluralize(@blog.errors.count, "error") %> prohibited
      this log from being saved:
    </h2>
    <ul>
      <% @blog.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
    </ul>
  </div>
<% end %>

  <div class="field">
    <%= f.label :content, "Type your content here" %><br />
    <%= f.text_area :content, size: "150x15" %>
  </div>

  <div class="actions">
    <%= f.submit "Save post" %>
  </div>
<% end %>

<%= link_to 'Back', blogs_path, method: :get %>
</div>
