<!DOCTYPE html>
<html>
<head>
  <title>Nutritionist's Friend</title>
  <%= javascript_include_tag "https://www.google.com/jsapi", "chartkick" %>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => false %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => false %>
  <%= csrf_meta_tags %>
</head>
<body>

  <header>
    <h1>Nutritionist's Friend</h1>
  </header>

  <div class="navigation">
  <% if current_user %>
    <h4><%= current_user.first_name + " " + current_user.last_name %></h4>
    <%= link_to 'Sign Out', destroy_user_session_path, method: :delete %>
    <%= link_to 'Edit Details', edit_user_registration_path, method: :get %>
  <% end %>
</div>

  <p class="notice"><%= notice %></p>
  <p class="alert"><%= alert %></p>

<%= yield %>

  <footer>
    <p>This site uses the <a href="https://ndb.nal.usda.gov/ndb/api/doc">
      USDA National Nutrient Database API</a></p>
  </footer>

</body>
</html>
